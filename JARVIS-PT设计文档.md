好的，这是将所有讨论整合并细化后的最终版产品设计文档。

### 一、 核心约束机制：惩罚与锁死系统

此系统是整个APP的底层行为准则。

-   **失格计数器**
    -   **机制**：内置隐藏计数器。每日结算时，若存在至少一项未完成的日常任务，计数+1；完成全部日常任务，计数即刻归零。
    -   **惩罚系数**：所有日常任务均附带固定、不可更改的惩罚系数。即，任一日常任务未完成，当天计数器必然+1。

-   **系统锁死**
    -   **触发条件**：计数器累积达到3天。
    -   **表现状态**：
        -   所有功能界面覆盖动态破碎玻璃特效，并模糊化底层数据。
        -   屏幕中央显示红色警告框：“警告：宿主白厄连续3天未达最低作战标准。系统已锁死。”
        -   底部仅保留唯一按钮：【申请重新编译】。

-   **重新编译仪式（双端验证）**
    -   **第一步：手机端发起**
        1.  点击【申请重新编译】，系统生成一个6位动态令牌（如：`A7X-29K`）和300秒倒计时。
        2.  界面提示：“正在生成临时令牌...请连接主终端，使用母块进行验证。”
    -   **第二步：电脑端验证**
        1.  在电脑上运行“宿主白厄·战术系统编译母块”程序（一个命令行工具）。
        2.  程序提示输入令牌。手动输入手机上的动态令牌并回车。
    -   **第三步：执行编译**
        1.  验证通过，手机端开始滚动白色编译代码，强制执行以下惩罚：
            -   扣除当前**通用经验值**的20%。
            -   扣除当前**所有潜能点**。
            -   全属性值降低1% - 3%（系统随机）。
        2.  编译完成，锁死解除，计数器归零。整个流程不可撤销，不留情面。

---

### 二、可配置任务引擎

在【任务】页右上角齿轮图标进入【任务配置中心】。

#### 1. 日常任务模板库与配置器
-   **管理**：可编辑、删除预置模板。
-   **创建**：点击“+创建日常任务”进入编辑器。
    -   **任务要求**：支持数值型、百分比型、时间型，并设置目标。
    -   **奖励设定**：从奖励池拖拽组合（通用经验、属性倾向、特定经验）。
    -   **惩罚系数**：**固定开启，不可更改**。确保所有日常任务都具备强制力。

#### 2. 主线/支线任务工坊（战术参谋API）
-   **创建方式一：手动创建**
    -   点击“+创建新任务”，选择【主线】或【支线】。
    -   手动设定任务结构（主线可设1-5个子目标）和名称。
-   **创建方式二：战术参谋API智能生成**
    -   **输入指令**：点击“启动战术参谋·生成任务”，输入你的目标（如“3个月内备战业余排球联赛”）。
    -   **生成草案与沟通调整**：
        1.  API返回一套结构化草案（包含任务名、子目标、奖励评估）。
        2.  你以草案为基础，通过界面与系统沟通，进行增、删、改，或让API“重新评估难度”以调整奖励。
        3.  直到大致满意。
    -   **自定义微调**：确认草案后，进入编辑器进行最终手动调整。
-   **奖励设定**：
    -   **主线任务**：根据难度由API或预设规则自动生成奖励（通用经验、潜能点、专长点、技能树解锁），宿主不直接参与数值编辑。
    -   **未知物品**：奖励池中可包含“加密数据包”，完成后方可解锁，增加惊喜感。
-   **激活与触发**：创建好的任务进入“待激活”列表，支持设置条件自动激活或手动激活。

---

### 三、周/月度战略报告

在【属性】页或【报告】标签页提供。

-   **周报（战况简报）**
    -   **标题**：“宿主白厄·第X周生存评估”
    -   **核心数据**：任务完成率环形图、属性变化对比雷达图。
    -   **事件时间线**：梳理本周关键成长节点与系统警告。

-   **月报（战术总结）**
    -   **标题**：“宿主白厄·X月进化月度报告”
    -   **趋势分析**：综合评分趋势折线图、属性构成堆叠图。
    -   **核心评价**：系统根据数据生成冷冰冰的评语（如“该作战单元展现出基础自律性”或“可靠性评级下调”）。

---

### 四、专属自用细节

-   **身份纯粹化**：启动验证“宿主ID：白厄”，全程强化单兵作战的代入感。
-   **数据完全透明**：在【背包】或设置页的【系统日志】中，以严谨格式（时间戳+事件）记录所有操作与系统警告。
-   **高级设置（底层编译室）**：
    -   **导出作战数据**：一键导出全量数据（JSON/CSV）。
    -   **手动触发系统锁死**：可主动触发锁死流程，用于重置进度或体验惩罚。
    -   **完全重置系统**：清空所有数据，需输入24位随机确认码。

---

### 五、整体数据流与交互

1.  **初始化**：启动APP，加载宿主白厄的所有状态数据。
2.  **任务驱动循环**：
    -   查看【日常/主线/支线】任务 -> 线下执行 -> 回APP签到 -> 系统校验 -> 发放奖励 -> 属性/资源即时更新。
3.  **监控与惩罚**：
    -   每日结算失败 -> 计数器+1 -> 达阈值则触发【系统锁死】-> 启动【双端编译流程】-> 执行惩罚 -> 解锁。
4.  **规划与复盘**：
    -   通过【战术参谋API】或手动创建规划任务。
    -   通过【战略报告】查看周/月数据，评估成长。

此方案整合了所有讨论细节，形成了一个功能完整、逻辑严密的个人战术管理系统。